# 功能规格说明书：用户管理与基于角色的访问控制

**功能分支**: `002-user-rbac`
**创建日期**: 2026-04-10
**状态**: 已澄清（含前端范围）
**输入**: 用户描述: "为Taskify增加用户管理，角色权限管理"

## 澄清记录

### 2026-04-10 会议

- Q: 现有系统已有 User 模型和预置用户数据（如张明、李伟等），新增用户管理功能应该如何处理这些已有数据？ → A: **替换迁移** - 废弃现有 User 表，创建全新的用户认证系统。预置用户需要重新创建。
- Q: 用户权限是否应该基于「项目成员资格」？ → A: **是，项目成员资格表** - 创建 project_memberships 表存储 用户-项目-角色 关系。用户在不同项目有不同角色。
- Q: 是否需要在本次功能中为所有现有接口添加权限验证？ → A: **是，全部添加权限校验** - 所有现有接口都需要验证用户登录状态和项目成员资格。Guest 只能查看，Member 可以操作任务，Owner/Admin 有管理权限。
- Q: 当前是否有邮件发送基础设施？ → A: **无，暂时跳过邮件功能** - 注册时邮箱标记为已验证（演示环境），密码找回暂不支持。后续有邮件基础设施时再补充。
- Q: 「项目管理」是指项目本身的 CRUD（创建、编辑、删除、归档）管理，还是仅指项目级权限控制？ → A: **完整 CRUD** - 包含项目的创建、编辑、删除、归档功能。项目创建者自动成为 Owner，拥有全部管理权限。
- Q: Project Owner 和系统管理员在成员管理上的分工是什么？ → A: **Owner 全权管理** - Project Owner 可以独立管理自己项目的所有成员（添加、移除、修改角色），无需系统管理员介入。系统管理员保留全局管理权限。
- Q: 新增用户认证系统需要前端做哪些页面修改？ → A: **全部新增/替换** - 新增登录页、注册页、个人资料页、管理员用户管理页、管理员角色管理页、替换现有 UserSelect 为登录页、所有页面添加认证守卫。
- Q: 管理员页面（用户管理、角色管理）应该使用什么布局？ → A: **独立管理后台** - 创建独立的管理后台界面（/admin 路径下），包含用户管理、角色管理、权限配置等独立菜单和页面。
- Q: 项目管理（创建、编辑、删除项目）和项目成员管理需要新建前端页面吗？ → A: **是，新增项目页面** - 新增项目管理页面（创建项目对话框、项目列表/卡片、项目设置、成员邀请/管理）。
- Q: 系统如何创建第一个系统管理员账户？ → A: **数据库种子脚本** - 系统启动时自动创建默认管理员账户（admin@taskify.local / admin123），使用环境变量覆盖凭据。
- Q: 批量导入用户支持什么文件格式？ → A: **Excel 文件** - 支持 .xlsx 格式，包含邮箱、显示名称两列。导入用户默认无全局角色，角色通过项目管理页面分配。
- Q: 批量导入时，用户的初始密码如何处理？ → A: **统一默认密码** - 使用固定默认密码（如 admin123），用户首次登录后强制修改。
- Q: 导入过程中遇到格式错误或数据冲突应该如何处理？ → A: **部分成功，报告失败项** - 跳过问题行继续处理，最后提供失败报告包含具体错误原因和行号。

### 2026-04-10 补充会议（用户管理增强）

- Q: 管理员重置用户密码后，新密码如何传递给用户？ → A: **重置为临时密码** - 管理员将密码重置为统一临时密码（如 admin123），用户首次登录时强制修改。
- Q: 禁用用户账号的具体行为是什么？ → A: **软禁用** - 设置 `is_disabled=true`，用户无法登录，但账号、项目成员资格、任务、评论数据全部保留，管理员可随时重新启用。
- Q: 管理员查询用户时需要支持哪些筛选条件？ → A: **多条件筛选分页** - 支持按邮箱、显示名称筛选；按系统角色筛选；按状态（正常/禁用）筛选；支持分页。

### 2026-04-10 补充会议（密码修改会话处理）

- Q: 用户修改密码后，其他已登录设备的会话应该如何处理？ → A: **仅保留当前会话** - 用户修改密码后，仅当前会话保持有效，其他设备的会话自动失效，需要重新登录。

## 用户场景与测试

### 用户故事 1 - 用户注册与登录 (优先级: P1)

作为一名新用户，我希望创建账户并登录，以便访问 Taskify 系统。

**优先级原因**: 没有身份认证，其他用户管理功能都无法运作。这是整个功能的基础。

**独立测试**: 可以通过创建新用户账户并验证登录/登出流程来完全测试，确保提供对系统的安全访问。

**验收场景**:

1. **假设** 我是一位新访客，**当** 我使用有效邮箱和密码注册时，**那么** 系统创建我的账户并自动登录。
2. **假设** 我已拥有账户，**当** 我使用正确凭据登录时，**那么** 我获得系统访问权限并看到我的项目列表。
3. **假设** 我已登录，**当** 我登出时，**那么** 我的会话被终止并被重定向到登录页面。
4. **假设** 我尝试使用无效凭据登录，**当** 我提交登录表单时，**那么** 我看到错误消息且无法访问系统。

---

### 用户故事 2 - 用户资料管理 (优先级: P2)

作为一名已登录用户，我希望管理我的个人资料信息，以便保持我的账户详细信息最新。

**优先级原因**: 用户需要维护准确的联系信息和偏好设置。

**独立测试**: 可以通过更新用户资料字段（姓名、邮箱、头像）并验证更改是否持久化来完全测试。

**验收场景**:

1. **假设** 我已登录，**当** 我进入我的个人设置页面，**那么** 我看到当前显示的信息。
2. **假设** 我已登录，**当** 我更新我的显示名称，**那么** 新名称在整个系统中显示。
3. **假设** 我已登录，**当** 我更改我的邮箱地址，**那么** 系统向新邮箱发送验证邮件。（注：当前版本跳过邮件发送，邮箱直接标记为已验证）
4. **假设** 我已登录，**当** 我更新我的密码，**那么** 系统在下次登录时要求使用新密码。

---

### 用户故事 3 - 角色管理 (优先级: P2)

作为系统管理员，我希望定义和管理角色，以便控制用户在系统中可以做什么。

**优先级原因**: 角色定义是权限系统的基础。没有定义的角色，权限就无法分配。

**独立测试**: 可以通过创建、编辑和删除自定义角色并验证角色属性被正确存储来完全测试。

**验收场景**:

1. **假设** 我是系统管理员，**当** 我查看角色列表时，**那么** 我看到所有系统预定义角色及其描述。
2. **假设** 我是系统管理员，**当** 我创建新的自定义角色时，**那么** 该角色出现在角色列表中。
3. **假设** 我是系统管理员，**当** 我编辑角色的名称或描述时，**那么** 更改被保存并在整个系统中反映。
4. **假设** 我是系统管理员，**当** 我删除未被任何用户分配的自定义角色时，**那么** 该角色从系统中移除。

---

### 用户故事 4 - 权限配置 (优先级: P2)

作为系统管理员，我希望配置每个角色可以做什么，以便实施最小权限原则。

**优先级原因**: 权限将角色转换为可操作的访问控制。这就是安全如何被执行。

**独立测试**: 可以通过向角色分配权限并验证具有该角色的用户具有预期访问权限来完全测试。

**验收场景**:

1. **假设** 我是系统管理员，**当** 我查看角色的权限时，**那么** 我看到所有可用权限及其当前分配情况。
2. **假设** 我是系统管理员，**当** 我向角色分配权限时，**那么** 具有该角色的用户现在可以执行允许的操作。
3. **假设** 我是系统管理员，**当** 我从角色移除权限时，**那么** 具有该角色的用户失去对该操作的访问权限。
4. **假设** 我是系统管理员，**当** 我查看所有可用权限时，**那么** 权限按资源类型分组（如用户、项目、任务、评论）。

---

### 用户故事 5 - 用户角色分配 (优先级: P1)

作为系统管理员，我希望向用户分配角色，以便他们具有适当的访问级别。

**优先级原因**: 没有角色分配，用户无法获得权限。这将用户连接到角色系统。

**独立测试**: 可以通过向用户分配角色并验证用户获得预期权限来完全测试。

**验收场景**:

1. **假设** 我是系统管理员，**当** 我查看用户详情时，**那么** 我看到他们当前的角色分配。
2. **假设** 我是系统管理员，**当** 我向用户分配角色时，**那么** 用户立即获得该角色的权限。
3. **假设** 我是系统管理员，**当** 我更改用户的角色时，**那么** 用户的权限更新为匹配新角色。
4. **假设** 我是系统管理员，**当** 我从用户移除角色时，**那么** 用户失去该角色的所有权限。

---

### 用户故事 6 - 项目管理 (优先级: P1)

作为已登录用户，我希望创建和管理项目，以便组织和协作工作内容。

**优先级原因**: 项目是任务和成员管理的容器，没有项目管理就无法组织工作。

**独立测试**: 可以通过创建新项目、编辑项目信息、删除项目并验证系统响应来完全测试。

**验收场景**:

1. **假设** 我已登录，**当** 我创建新项目并填写名称和描述时，**那么** 系统创建项目并自动将我分配为项目 Owner。
2. **假设** 我是项目 Owner，**当** 我编辑项目名称或描述时，**那么** 更改被保存并反映在项目详情中。
3. **假设** 我是项目 Owner，**当** 我删除项目时，**那么** 系统要求确认，确认后项目及其所有任务被永久删除。
4. **假设** 我是项目 Owner，**当** 我归档项目时，**那么** 项目被标记为已归档，不再出现在活动项目列表中，但数据保留。

---

### 用户故事 7 - 项目成员邀请与管理 (优先级: P1)

作为项目 Owner，我希望邀请和管理项目成员，以便团队协作。

**优先级原因**: 项目需要多个成员参与，Owner 需要能够添加和移除成员。

**独立测试**: 可以通过添加项目成员、更改成员角色、移除成员并验证权限变化来完全测试。

**验收场景**:

1. **假设** 我是项目 Owner，**当** 我通过邮箱单个或批量添加成员到项目时，**那么** 系统直接添加成员（支持单条和批量方式），成员立即获得对应角色权限。
2. **假设** 我是项目 Owner，**当** 我将成员角色从 Member 改为 Guest 时，**那么** 该成员权限立即更新为 Guest 级别。
3. **假设** 我是项目 Owner，**当** 我从项目中移除成员时，**那么** 该成员失去对项目的所有访问权限，但他们的任务和评论保留。
4. **假设** 我是项目 Member 或 Guest，**当** 我查看项目成员列表时，**那么** 我看到所有成员及其各自角色。

---

### 用户故事 8 - 前端登录与注册 (优先级: P1)

作为新用户，我希望通过前端页面注册和登录，以便访问系统功能。

**优先级原因**: 前端登录/注册是用户进入系统的入口，没有前端认证流程就无法使用系统。

**独立测试**: 可以通过前端表单完成注册、登录、登出并验证页面跳转和状态更新。

**验收场景**:

1. **假设** 我是新访客，**当** 我访问系统根路径时，**那么** 系统显示登录页面而非 UserSelect 页面。
2. **假设** 我是新访客，**当** 我点击注册链接并填写有效信息后提交，**那么** 系统创建账户并自动登录，重定向到项目列表页面。
3. **假设** 我已注册，**当** 我输入正确邮箱和密码后点击登录，**那么** 系统验证通过并重定向到我的项目列表。
4. **假设** 我已登录，**当** 我点击登出按钮，**那么** 会话终止，页面重定向到登录页面。

---

### 用户故事 9 - 管理员后台 - 用户管理 (优先级: P1)

作为系统管理员，我希望通过前端页面管理所有用户，以便查看和配置用户账户。

**优先级原因**: 管理员需要可视化界面来管理用户，无法仅通过 API 操作。

**独立测试**: 可以通过前端页面完成用户列表查看、用户角色分配。

**验收场景**:

1. **假设** 我是系统管理员，**当** 我访问 /admin/users 路径时，**那么** 系统显示用户管理页面，包含所有用户列表。
2. **假设** 我是系统管理员，**当** 我在用户管理页面选择用户并修改其角色后保存，**那么** 系统更新用户角色并显示成功消息。

---

### 用户故事 9b - 批量导入用户 (优先级: P2)

作为系统管理员，我希望批量导入用户，以便快速创建多个账户。

**优先级原因**: 当需要一次性创建大量用户（如团队成员）时，逐个注册效率低下。

**独立测试**: 可以通过上传 Excel 文件并验证导入结果（成功数量、失败报告）来完全测试。

**验收场景**:

1. **假设** 我是系统管理员，**当** 我下载批量导入模板并填写用户信息（邮箱、显示名称）后上传，**那么** 系统解析 Excel 文件并创建账户。
2. **假设** 我上传的 Excel 中部分数据格式错误，**当** 系统处理时，**那么** 跳过错误行，成功导入其他有效数据，并生成失败报告。
3. **假设** 我是系统管理员，**当** 批量导入完成后，**那么** 导入的用户收到统一初始密码（admin123），首次登录时必须修改密码。

---

### 用户故事 9c - 用户查询、重置密码与禁用 (优先级: P2)

作为系统管理员，我希望查询用户、重置密码和禁用账号，以便对用户账户进行全面管理。

**优先级原因**: 管理员需要能够处理日常用户账户问题，包括密码重置和问题账号的紧急禁用。

**独立测试**: 可以通过管理员后台的用户管理页面完成用户搜索、重置密码、禁用/启用账号操作。

**验收场景**:

1. **假设** 我是系统管理员，**当** 我在用户管理页面输入邮箱或显示名称进行搜索时，**那么** 系统返回匹配的用户列表。
2. **假设** 我是系统管理员，**当** 我选择筛选条件（如角色、账号状态）时，**那么** 系统返回符合条件用户列表。
3. **假设** 我是系统管理员，**当** 我点击重置密码按钮并确认后，**那么** 用户密码被重置为临时密码（admin123），系统显示成功消息。
4. **假设** 用户收到临时密码，**当** 用户首次登录时，**那么** 系统强制要求用户设置新密码。
5. **假设** 我是系统管理员，**当** 我点击禁用账号按钮并确认后，**那么** 该用户无法再登录系统，但其项目成员资格、任务和评论数据保留。
6. **假设** 被禁用的用户尝试登录，**当** 用户提交登录表单时，**那么** 系统显示账号已被禁用的错误消息。
7. **假设** 我是系统管理员，**当** 我对已禁用的用户点击启用账号按钮，**那么** 该用户可以恢复正常登录状态。

---

### 用户故事 10 - 管理员后台 - 角色与权限管理 (优先级: P2)

作为系统管理员，我希望通过前端页面配置角色和权限，以便可视化管理系统权限。

**优先级原因**: 角色和权限配置是安全核心，需要可视化界面辅助配置。

**独立测试**: 可以通过前端页面完成角色列表查看、权限分配、角色创建/编辑/删除。

**验收场景**:

1. **假设** 我是系统管理员，**当** 我访问 /admin/roles 路径时，**那么** 系统显示角色管理页面，列出所有预定义和自定义角色。
2. **假设** 我是系统管理员，**当** 我编辑某角色的权限并保存，**那么** 系统更新角色权限绑定并立即对具有该角色的用户生效。

---

### 用户故事 11 - 项目管理前端 (优先级: P1)

作为已登录用户，我希望通过前端页面管理项目和项目成员，以便通过可视化界面组织工作。

**优先级原因**: 项目管理是核心功能，需要前端界面支持创建、编辑、归档项目和邀请成员。

**独立测试**: 可以通过前端页面完成项目 CRUD 和成员管理操作。

**验收场景**:

1. **假设** 我已登录，**当** 我点击新建项目按钮并填写信息后提交，**那么** 系统创建项目并自动将我分配为 Owner，页面跳转或刷新显示新项目。
2. **假设** 我是项目 Owner，**当** 我进入项目设置页面，**那么** 我可以编辑项目基本信息、查看/管理项目成员。
3. **假设** 我是项目 Owner，**当** 我点击归档项目按钮并确认，**那么** 项目被归档并从活动项目列表中移除。

---

### 用户故事 12 - 前端访问控制执行 (优先级: P1)

作为前端用户，我希望系统根据我的角色显示和禁用相应操作，以便我不会看到或尝试无权执行的操作。

**优先级原因**: 前端权限控制提升用户体验，避免用户看到无权限的按钮后点击导致后端拒绝。

**独立测试**: 可以通过使用不同角色登录并验证页面元素可见性和禁用状态。

**验收场景**:

1. **假设** 我是 Guest 登录，**当** 我查看项目看板页面，**那么** 我看不到新建任务、编辑任务按钮，只能查看。
2. **假设** 我是 Member 登录，**当** 我查看项目看板页面，**那么** 我可以创建和编辑任务，但看不到删除项目按钮。
3. **假设** 我未登录，**当** 我直接访问项目页面 URL，**那么** 系统重定向我到登录页面。

作为用户，我希望系统强制执行访问控制，以便我只能执行角色允许的操作。

**优先级原因**: 这是核心安全机制。没有执行，权限系统毫无意义。

**独立测试**: 可以通过尝试使用不足权限执行操作并验证它们被阻止来完全测试。

**验收场景**:

1. **假设** 我是普通成员用户，**当** 我尝试访问管理功能时，**那么** 系统拒绝访问并显示错误。
2. **假设** 我是项目成员，**当** 我尝试删除项目时，**那么** 系统拒绝访问（只有项目所有者或管理员可以）。
3. **假设** 我已登出，**当** 我尝试访问任何受保护页面时，**那么** 系统将我重定向到登录页面。
4. **假设** 我是项目的访客用户，**当** 我尝试创建任务时，**那么** 系统拒绝访问（访客只能查看）。

---

### 边界情况

- 当管理员尝试删除自己的账户时会发生什么？
- 当最后一个系统管理员从管理员角色中被移除时会怎样？
- 当用户的角色在登录时被删除会发生什么？
- 系统如何处理同一用户的并发权限更新？
- 当用户尝试使用已存在的邮箱注册时会怎样？
- 当非成员用户尝试直接通过 API URL 访问项目时会发生什么？

## 需求

### 功能需求

- **FR-001**: 系统必须允许新用户使用邮箱和密码注册。
- **FR-002**: 系统必须通过邮箱和密码凭据对用户进行身份认证。
- **FR-003**: 系统必须允许用户登出并终止其会话。
- **FR-004**: 系统必须允许用户更新自己的个人资料（姓名、头像）。前端必须提供个人资料页面。
- **FR-005**: 系统必须允许用户在当前密码验证后更改密码。更改密码后，仅当前会话保持有效，其他设备的会话自动失效。
- **FR-006**: 系统必须提供预定义角色，按作用域分为两类：系统级角色「系统管理员」（scope=system），以及项目级角色「项目所有者、成员、访客」（scope=project）。项目级角色通过"加入项目"操作分配，不能全局分配。
- **FR-007**: 系统必须允许管理员创建具有描述性名称的自定义角色（scope=project）。
- **FR-008**: 系统必须允许管理员删除未被任何用户分配的自定义角色。
- **FR-009**: 系统不得允许删除预定义角色（系统管理员、项目所有者、成员、访客）。
- **FR-010**: 系统必须定义按资源分组的权限：用户（查看、管理）、项目（查看、创建、编辑、删除）、任务（查看、创建、编辑、移动、删除）、评论（查看、创建、编辑、删除）。
- **FR-011**: 系统必须允许管理员向角色分配任何组合的权限。
- **FR-012**: 系统必须允许管理员向用户分配系统级角色（scope=system）。项目级角色（scope=project）通过"项目成员邀请/管理"操作分配，不得通过管理员角色分配接口全局分配。
- **FR-013**: 系统必须基于用户角色权限在所有受保护操作上强制执行访问控制。
- **FR-014**: 系统必须拒绝非管理员用户访问管理功能。
- **FR-015**: 系统必须在访问受保护资源时将未认证用户重定向到登录页面。
- **FR-016**: 系统必须阻止非成员用户访问他们不是成员的项目，除非他们是管理员。
- **FR-017**: 系统必须记录安全相关事件（登录、登出、权限更改、失败的访问尝试）。
- **FR-018**: 系统必须基于用户身份认证和项目成员资格对所有现有 API 端点（项目、任务、评论）进行访问控制改造。
  - **注意**: 此需求在 MVP 阶段（US1-US12）暂不实现，待 Phase 1-12 完成后在 Phase 13 中进行。
- **FR-019**: 系统必须允许已登录用户创建新项目，并在创建时自动将该用户分配为项目 Owner。
- **FR-020**: 系统必须允许项目 Owner 编辑项目名称和描述。
- **FR-021**: 系统必须允许项目 Owner 删除项目，删除前要求确认，删除后项目及其所有任务永久移除。
- **FR-022**: 系统必须允许项目 Owner 归档项目，归档后项目不显示在活动项目列表中但数据保留。
- **FR-023**: 系统必须允许项目 Owner 在自己的项目内添加、移除成员并修改成员角色，无需系统管理员介入。
- **FR-023a**: 系统必须支持项目 Owner 批量添加成员（最多 50 人一次），以邮箱列表和角色ID批量创建项目成员关系。
- **FR-024**: 前端必须提供登录页面，支持邮箱和密码输入及提交。
- **FR-025**: 前端必须提供注册页面，支持新用户创建账户。
- **FR-026**: 前端必须在所有页面添加认证守卫，未登录用户访问受保护路径时重定向到登录页。
- **FR-027**: 前端必须提供 /admin 路径下的管理后台，包含用户管理、角色管理、权限配置页面。
- **FR-028**: 前端必须根据用户角色显示/隐藏操作按钮和菜单项（Guest 隐藏创建/编辑/删除，Member 隐藏项目删除等）。
- **FR-029**: 前端必须提供项目管理页面，支持创建、编辑、归档项目和项目成员邀请/管理。
- **FR-030**: 系统必须支持管理员通过 Excel 文件批量导入用户。
- **FR-031**: 系统必须支持 Excel 文件格式 .xlsx，包含邮箱、显示名称两列。导入用户默认无全局角色，角色通过项目管理页面分配。
- **FR-032**: 批量导入时，系统必须使用统一默认密码（admin123），用户首次登录后强制修改。
- **FR-033**: 批量导入时遇到格式错误或数据冲突（如邮箱已存在），系统必须跳过错误行并继续处理，最终生成失败报告包含行号和错误原因。
- **FR-034**: 系统必须支持管理员查询用户，支持按邮箱、显示名称、角色、状态（正常/禁用）筛选，并支持分页。
- **FR-035**: 系统必须允许管理员重置用户密码，重置后用户密码变为临时密码（admin123），用户首次登录时强制修改。
- **FR-036**: 系统必须允许管理员禁用用户账号，禁用后用户无法登录，但账号、项目成员资格、任务、评论数据保留。
- **FR-037**: 系统必须允许管理员重新启用已禁用的用户账号，恢复其正常登录状态。
- **FR-038**: 系统必须支持项目列表分页显示，每页默认 20 条，最大不超过 100 条，支持按页码跳转。
- **FR-039**: 系统必须支持项目列表按名称关键字模糊搜索（子串匹配）、按状态（活动/已归档）筛选、按项目所有者名称（owner_name）模糊匹配筛选，多条件为 AND 组合。
- **FR-040**: 任务卡片必须显示项目所有者信息（显示名称和头像）。

### 关键实体

- **User（用户）**: 表示系统用户。属性：唯一标识符、邮箱地址、显示名称、头像 URL、密码哈希、创建时间戳、最近登录时间戳、邮箱已验证（布尔值）、已禁用（布尔值）。关系：可通过 ProjectMembership 分配给多个项目。
- **Role（角色）**: 表示一组权限。属性：唯一标识符、名称、描述、是否系统预定义（布尔值）、创建时间戳。关系：可通过 ProjectMembership 分配给多个用户，包含多个权限绑定。
- **Permission（权限）**: 表示系统可以授权的操作。属性：唯一标识符、资源类型、操作名称（如 "tasks.delete"）。
- **RolePermission（角色权限）**: 角色和权限之间的多对多关系。属性：角色 ID、权限 ID。
- **ProjectMembership（项目成员资格）**: 用户与项目之间带角色的多对多关系。属性：用户 ID、项目 ID、角色 ID、加入时间。这是按项目访问控制的核心。
- **AuditLog（审计日志）**: 记录安全相关事件。属性：时间戳、用户 ID、事件类型、详情、IP 地址。

### 约束与原则

- **单一所有者原则**: 每个项目只能有一位所有者（Owner）。项目创建时，创建者自动成为 Owner（通过 `project_memberships` 表分配 `roles.name='owner'`）。变更所有者时，系统自动将原所有者降级为成员（角色变更为 Member）。

## 成功标准

### 可衡量成果

- **SC-001**: 注册 API 响应时间 < 5秒（不包括前端渲染和网络延迟）。
- **SC-002**: 登录 API 响应时间 < 2秒（不包括前端渲染和网络延迟）。
- **SC-003**: 权限拒绝 API 响应时间 < 500ms（p95），在测试环境验证 100% 覆盖。
- **SC-004**: 角色和权限更改立即对后续请求生效（无缓存）。
- **SC-005**: 角色创建 API 响应时间 < 3秒。
- **SC-006**: 100% 的安全事件被记录，包含用户ID、事件类型、时间戳、IP。
- **SC-007**: 用户创建 API 响应时间 < 3秒。

## 假设条件

- **认证方式**: 基于邮箱和密码的身份认证与会话管理。这是内部工具（如 Taskify）的标准方法。
- **会话管理**: 服务器端会话，会话令牌存储在 HTTP-only Cookie 中。这比本地存储替代方案提供更好的安全性。
- **密码要求**: 最少 8 个字符，至少一个字母和一个数字。这在安全性和可用性之间取得平衡。
- **角色范围**: 角色在两个级别运作 — 系统管理员（scope=system，全局访问）和项目级角色（scope=project，包括所有者、成员、访客，决定用户在特定项目内可以做什么）。所有角色都存储在 `roles` 表，通过 `role_scope` 字段区分。角色分配统一使用 `ProjectMembership` 表：系统管理员分配时 `ProjectID=0`，项目成员分配时 `ProjectID=项目ID`。
- **多项目成员资格**: 用户可以通过 ProjectMembership 实体在不同项目具有不同角色。例如，用户在项目 A 中可能是成员，在项目 B 中是所有者。
- **单一管理员**: 系统中必须始终至少有一名系统管理员。系统将阻止移除最后一个管理员。系统启动时通过数据库种子脚本自动创建默认管理员账户。
- **默认管理员凭据**: 默认管理员邮箱 `admin@taskify.local`，密码 `admin123`。可通过环境变量 `ADMIN_EMAIL` 和 `ADMIN_PASSWORD` 覆盖。生产环境必须修改默认密码。
- **数据迁移**: 现有 User 表将被替换。预定义用户（张明、李伟等）将通过新凭据迁移或重新创建。
- **改造现有处理器**: 所有现有 API 处理器（项目、任务、评论）将添加身份认证和授权中间件进行改造。
- **邮箱验证**: 延期。注册时将邮箱标记为已验证以用于演示。密码重置和邮箱更改验证延期至 SMTP 基础设施可用时。
- **项目管理**: 完整 CRUD。项目创建者自动成为 Owner。Project Owner 可独立管理自己项目的所有成员，无需系统管理员介入。
- **前端范围**: 全部新增/替换。新增登录页、注册页、个人资料页、管理员后台（/admin）、项目管理页面。所有页面添加认证守卫。复用现有看板组件。
